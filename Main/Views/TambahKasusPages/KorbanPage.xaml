<Page x:Class="Main.Views.TambahKasusPages.KorbanPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:main="clr-namespace:Main"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
     Background="{DynamicResource MaterialDesignPaper}"
     FontFamily="{DynamicResource MaterialDesignFont}"
      Title="KorbanPage">
    <Page.Resources>
        <main:EnumToBoolConverter x:Key="EBConverter"/>
    </Page.Resources>
    <StackPanel>
        <GroupBox >
            <GroupBox.HeaderTemplate>
                <DataTemplate>
                    <Grid Margin="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80*"/>
                            <ColumnDefinition Width="20*"/>
                        </Grid.ColumnDefinitions>
                        <Label Width="auto" Content="Korban" HorizontalAlignment="Left" Foreground="White"/>
                        <WrapPanel HorizontalAlignment="Right" Grid.Column="1">
                            <Button  Style="{StaticResource MaterialDesignFloatingActionAccentButton}" Width="25" Height="25">
                                <materialDesign:PackIcon
                                Kind="Add"
                                Height="24"
                                Width="24" />               
                            </Button>
                        </WrapPanel>
                    </Grid>
                </DataTemplate>
            </GroupBox.HeaderTemplate>
            <DataGrid>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="No"/>
                    <DataGridTextColumn Header="Nama"/>
                    <DataGridTextColumn Header="JK"/>
                    <DataGridTextColumn Header="Agama"/>
                    <DataGridTextColumn Header="Pendidikan"/>
                    <DataGridTextColumn Header="Suku"/>
                    <DataGridTextColumn Header="Alamat" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
        <GroupBox Header="Pelaku">
            <GroupBox.HeaderTemplate>
                <DataTemplate>
                    <Grid Margin="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80*"/>
                            <ColumnDefinition Width="20*"/>
                        </Grid.ColumnDefinitions>
                        <Label Width="auto" Content="Pelaku" HorizontalAlignment="Left" Foreground="White"/>
                        <WrapPanel Grid.Column="1" HorizontalAlignment="Right">
                            <Button  Style="{StaticResource MaterialDesignFloatingActionAccentButton}" Width="30" Height="30" >
                                <materialDesign:PackIcon
                                Kind="Add"
                                Height="24"
                                Width="24" />
                            </Button>
                        </WrapPanel>
                    </Grid>
                </DataTemplate>
            </GroupBox.HeaderTemplate>
            <DataGrid>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="No"/>
                    <DataGridTextColumn Header="Nama"/>
                    <DataGridTextColumn Header="JK"/>
                    <DataGridTextColumn Header="Agama"/>
                    <DataGridTextColumn Header="Pendidikan"/>
                    <DataGridTextColumn Header="Suku"/>
                    <DataGridTextColumn Header="Alamat" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </StackPanel>
</Page>
